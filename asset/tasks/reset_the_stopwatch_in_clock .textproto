reset_steps:[
  { 
    adb_request: { 
      press_button: { 
        button: HOME  
      }
    }
  }
]

max_episode_steps: 5

log_parsing_config: {
  filters: ["AlarmClock:D"]
  log_regexps: {
    reward_event: {
        event: "^(.*?)Reset"
        reward: 1
    }
  }
}
